/*-----------------------------------------------------------------------------
>>> «CHECKBOX» COMPONENT
-----------------------------------------------------------------------------*/

Satus.components.checkbox = function(object) {
    var component = document.createElement('input');

    component.type = 'checkbox';

    return component;
};